# 图像特征检测与匹配实验报告

## 1. 实验目的

通过实现边缘检测、特征点检测、图像匹配与全景拼接，理解从低层视觉特征到几何变换估计、再到图像融合的完整流程。

## 2. 实验环境

- 操作系统：Windows
- 应用形式：Web 交互式实验平台
- 核心文件：`index.html`、`styles.css`、`app.js`、`vision_core.py`

## 3. Prompt（文本）

见 [prompt.txt](C:\Users\27260\Desktop\研一第二学期课程\A3\prompt.txt)。

## 4. 功能实现说明

### 4.1 Canny 边缘检测

- 对输入图像进行灰度化。
- 使用高斯滤波抑制噪声。
- 使用 Sobel 算子计算梯度幅值与方向。
- 进行非最大值抑制。
- 进行双阈值检测与滞后连接，得到最终边缘。

截图位置：

- 原图：
- 梯度幅值图：
- 非最大值抑制后：
- 最终边缘图：

### 4.2 Harris / SIFT 特征点检测

- Harris：基于二阶矩阵计算角点响应，并通过局部极值筛选特征点。
- SIFT 风格：构建尺度空间差分，检测极值点，并生成 128 维描述子。

截图位置：

- Harris 特征点：
- SIFT 特征点：

### 4.3 两图匹配流程可视化

- 提取两幅图像的 SIFT 风格关键点与描述子。
- 采用最近邻与比值检验获得初始匹配。
- 使用 RANSAC 估计单应矩阵并筛选内点。
- 将图像变换到同一坐标系，展示匹配对齐效果。

截图位置：

- 特征点检测与描述：
- 初始匹配：
- RANSAC 内点：
- 变换与对齐：

### 4.4 多图全景拼接

- 对多张重叠图像进行相邻匹配。
- 估计各图像到中心图像的单应关系。
- 对所有图像执行反向映射与投影。
- 对比 `overlay`、`average`、`linear feather` 三种 blending 效果。

截图位置：

- Panorama：
- Blending 对比：

## 5. 结果分析

### 5.1 边缘检测效果分析

填写建议：

- 分析高低阈值变化对细节边缘和噪声边缘的影响。
- 对比非最大值抑制前后边缘宽度变化。

### 5.2 特征点检测效果分析

填写建议：

- Harris 更偏向角点；SIFT 风格方法对尺度变化更鲁棒。
- 可结合图像纹理丰富程度分析特征点数量变化。

### 5.3 图像匹配与 RANSAC 分析

填写建议：

- 讨论误匹配产生原因。
- 说明 RANSAC 如何剔除离群点并提升对齐效果。

### 5.4 Panorama 与 Blending 分析

填写建议：

- 说明不同 blending 的边界过渡差异。
- 讨论重叠区域不足或视差较大时的拼接问题。

## 6. 实验小结

可从以下角度总结：

- 掌握了经典视觉流程：检测、描述、匹配、估计、拼接。
- 认识到参数设置对结果稳定性影响较大。
- 通过交互界面可以更直观地观察各处理阶段结果。

## 7. 附录

- 源码文件清单
- 关键截图
- 可选外链 URL
