# 半监督双重稳健Kink回归模型处理效应检验方法研究
## 开题报告演讲稿（8-10分钟）

---

**各位老师好！**

我是统计与数据科学学院的李婧漪，我的论文题目是《半监督双重稳健Kink回归模型处理效应检验方法研究》。下面我将从选题背景与研究问题、模型与方法、研究创新、模拟实验结果以及后续研究计划五个方面进行汇报。

---

### 一、选题背景与研究问题（2分钟）

首先介绍选题背景。

**处理效应异质性检验**是因果推断领域的核心问题之一。传统的处理效应估计通常假设效应是恒定的，但在实际应用中，处理效应往往随着某个连续变量的变化而发生结构性改变。例如，在教育政策评估中，补贴政策的效果可能仅在家庭收入超过某个阈值后才显著；在医学研究中，某种药物的疗效可能仅在患者年龄或生物标志物超过某个临界值时才显现。这种"拐点效应"（Kink Effect）的检验具有重要的理论和实践意义。

如图所示，kink回归指的是在特定点斜率变化的回归模型，与传统的断点回归不一致的是，断点回归是两段不连续，而kink回归是两段连续但斜率不同。

（Kink回归：在拐点处连续但斜率变化。模型形式为
$$
Y = \alpha + \beta X + \tau (X-c)\mathbf{1}(X \geq c) + \varepsilon
$$

$\tau$代表斜率变化多少，c就是拐点）

与此同时，大数据时代我们普遍面临**标签稀缺问题**。在很多实际场景中，协变量信息和处理分配信息可以大规模、低成本地获取，但结果变量的收集往往成本高昂。例如，在医学随访研究中，患者的基线特征和治疗信息容易记录，但最终的健康结局需要长期追踪才能获得。这就形成了一种**半监督数据结构**：少量的有标签数据与大量的无标签数据并存。

一个自然的问题是：能否利用无标签数据中的协变量信息来提升检验效率？这正是本研究的核心动机。

基于此，本文聚焦三个核心研究问题：

1. 如何构建**半监督双重稳健**检验统计量？目标是当倾向分数模型或结果模型至少一个正确设定时，保证Type I Error的有效控制。

2. 如何通过**非参数插补**方法融入无标签数据？目标是实现渐近方差的缩减，从而提升检验功效。

3. 如何设计高效的**乘数自助法**？目标是在大规模无标签数据下避免计算瓶颈。

---

### 二、模型与方法（4分钟）

接下来介绍模型设定和方法框架，我会从"为什么这样设计"的角度来讲解。

**首先看模型设定**。

## 模型设定

我们考虑半监督Kink回归模型：

$$
Y_i=\alpha_0+\alpha_1X_i+\tau A_i(X_i-\delta)\mathbf{1}(X_i\geq\delta)+\mu(W_i)+\varepsilon_i
$$


逐项解释：
- X是连续运行变量，比如年龄、收入、生物标志物
- δ是未知的拐点位置，需要估计
- τ是拐点处斜率的变化量，这是我们最关心的核心参数
- A是处理变量，为什么要乘a?因为要研究处理效应是否在某个阈值后发生变化

μ(W)是协变量的非线性效应，为位置非参数函数

数据结构方面，我们有标记数据集L，包含n个样本，每个样本有完整的(X, W, A, Y)信息；同时有未标记数据集U，包含N个样本，但缺失Y。关键假设是N远大于n。

研究目标是检验
$$
H_0:\tau=0
\qquad
\text{vs}
\qquad
H_1:\tau\neq0.
$$
即是否存在拐点效应。

如果采用wald检验，需要估计$(\tau,\delta)$，最大的问题是拐点δ未知，属于非规则参数，因此$\hat\delta$往往不满足普通$\sqrt n$渐进理论，导致$\hat\tau$的极限分布难以推导，所以我们的思路是采用Score检验，score检验的的思路是：不直接估计τ，而是构造一个在$\tau=0$时期望为零的函数

## score检验

（一个直观的例子：

假设：
$$
Y = \alpha + \tau X + \varepsilon
$$

  如果：
$$
  \tau = 0
$$

  那么：
$$
  Y = \alpha + \varepsilon
$$

  此时 $X$ 和残差 $Y - \alpha$ 应该没关系。

  即：
  $$
  E[X(Y-{\alpha})]=0
  $$
  
  于是构造：
$$
  S = \frac{1}{n}\sum_{i=1}^{n} X_i (Y_i - \hat{\alpha})
$$
  
  如果 $S \approx0$，说明 $\tau = 0$ 可能成立。

因此，在$H_0:\tau=0$条件下，模型退化为$Y_i=\alpha_0+\alpha_1X_i+\mu(W_i)+\varepsilon_i.$，定义
$$
h(X_i,W_i)
=
E(Y_i\mid X_i,W_i).
$$

即是计算
$$
E\{Y_i-h(X_i,W_i)\mid X_i,W_i\}=0.
$$

因此对于任意函数$g(X_i,A_i)$，都有
$$
E\left[
g(X_i,A_i)
\{Y_i-h(X_i,W_i)\}
\right]
=0.
$$
为了识别kink效应，选择
$g(X_i,A_i)=A_i(X_i-\delta)\mathbf1(X_i\ge\delta).$,得到矩条件
$$
E
\Big[
A_i(X_i-\delta)\mathbf1(X_i\ge\delta)
\{Y_i-h(X_i,W_i)\}
\Big]
=0.
$$

## 引入倾向分数

$$
A_i(X_i-\delta)\mathbf1(X_i\ge\delta)
\{Y_i-h(X_i,W_i)\}
$$
的估计要求$h(X,W)$估计正确，否则$E[\psi]\neq0.$检验失败，而现实中h往往容易设错，$h(X,W)$含义是，给一个特征$(X,W)$，预测他的平均结果$Y$。（预测Y，Y-h是结果残差）

引入倾向分数，即定义
$$
\pi(X_i,W_i)
=
P(A_i=1\mid X_i,W_i).
$$
为倾向分数，含义是在给定协变量$(X,W)$条件下，一个样本接受处理$(A=1)$的概率（即预测A，A-pi是处理残差），模型设定改变为

ψ₁ = (X-δ)1(X≥δ) · {A - π̂} · {Y - ĥ}

每一项都有明确含义：
- 第一项(X-δ)1(X≥δ)：拐点识别权重，只在拐点右侧起作用，放大拐点信号
- 第二项{A - π̂}：处理残差，消除选择偏差，处理非随机化问题
- 第三项{Y - ĥ}：结果残差，控制协变量效应，提高估计精度

对于倾向分数来说，因为A是二分类变量，因此有
$$
E[A_i-\pi(X_i,W_i)\mid X_i,W_i]
=0.
$$

该性质，意味着$A-\pi$与所有$X,W$函数都正交，可以保证h和pi只要有一个正确，模型设定仍然正确，使模型具备双重稳健性质。
### 如何推理出来这个性质：



考虑 Score 函数

$$
\psi=(A-\hat\pi(X,W))(Y-\hat h(X,W)).
$$

其中

$$
\pi(X,W)=P(A=1\mid X,W)
$$

为倾向分数，

$$
h(X,W)=E(Y\mid X,W)
$$

为结果回归函数。

双重稳健（Double Robustness）指的是：

$$
E[\psi]=0
$$

只需要满足以下两个条件中的任意一个：

$$
\hat h = h
$$

或

$$
\hat\pi = \pi.
$$

即使另一个模型被错误设定，Score 的均值仍然保持为零。

---

### 情形一：结果模型正确，倾向分数模型错误

假设

$$
\hat h(X,W)=h(X,W)=E(Y\mid X,W).
$$

则

$$
Y-h(X,W)
$$

是真实残差。

根据条件期望定义，

$$
E[Y-h(X,W)\mid X,W,A]=0.
$$

此时

$$
E[\psi]
=
E[(A-\hat\pi)(Y-h)].
$$

利用迭代期望，

$$
E[\psi]
=
E\Big[
E[(A-\hat\pi)(Y-h)\mid X,W,A]
\Big].
$$

由于

$$
A-\hat\pi
$$

在给定

$$
(X,W,A)
$$

后已经确定，

因此可以提出条件期望：

$$
=
E\Big[
(A-\hat\pi)
E[(Y-h)\mid X,W,A]
\Big].
$$

而

$$
E[(Y-h)\mid X,W,A]
=
0.
$$

因此

$$
E[\psi]
=
0.
$$

即使

$$
\hat\pi
$$

完全错误，

Score 的均值仍然为零。

---

### 情形二：倾向分数模型正确，结果模型错误

假设

$$
\hat\pi(X,W)=\pi(X,W).
$$

此时允许

$$
\hat h
$$

错误。

定义结果模型误差

$$
r(X,W)
=
h(X,W)-\hat h(X,W).
$$

于是

$$
Y-\hat h
=
Y-h+r.
$$

代入 Score：

$$
\psi
=
(A-\pi)(Y-h+r).
$$

展开：

$$
\psi
=
(A-\pi)(Y-h)
+
(A-\pi)r.
$$

因此

$$
E[\psi]
=
E[(A-\pi)(Y-h)]
+
E[(A-\pi)r].
$$

下面分别分析两项。

---

第一项：

$$
E[(A-\pi)(Y-h)].
$$

利用迭代期望，

$$
=
E\Big[
(A-\pi)
E[(Y-h)\mid X,W]
\Big].
$$

而

$$
h(X,W)
=
E(Y\mid X,W),
$$

所以

$$
E[(Y-h)\mid X,W]
=
0.
$$

因此

$$
E[(A-\pi)(Y-h)]
=
0.
$$

---

第二项：

$$
E[(A-\pi)r(X,W)].
$$

由于

$$
r(X,W)
$$

仅仅是

$$
(X,W)
$$

的函数。

利用迭代期望，

$$
E[(A-\pi)r]
=
E\Big[
r(X,W)
E[A-\pi\mid X,W]
\Big].
$$

而根据倾向分数定义，

$$
E[A-\pi(X,W)\mid X,W]
=
0.
$$

所以

$$
E[(A-\pi)r]
=
0.
$$

最终得到

$$
E[\psi]
=
0+0
=
0.
$$

因此即使

$$
\hat h
$$

完全错误，

只要

$$
\hat\pi
$$

正确，

Score 的均值仍然为零。

---

### 为什么说倾向分数能够修复结果模型误差？

假设结果模型误差为

$$
r(X,W).
$$

那么由于

$$
r(X,W)
$$

只是

$$
(X,W)
$$

的函数，

而

$$
A-\pi(X,W)
$$

满足

$$
E[A-\pi(X,W)\mid X,W]=0,
$$

因此

$$
E[(A-\pi)r(X,W)]
=
0.
$$

也就是说，

倾向分数残差会自动消除任何仅依赖于

$$
(X,W)
$$

的模型误差。

这就是双重稳健性的数学来源。

---



## 半监督扩展

核心问题是：未标记数据缺失Y，如何利用？

解决方案是非参数插补。对于未标记数据，我们用NW核估计从协变量信息中插补Ŷ：

$$
\hat Y_j
=
\frac{
\sum_{i=1}^{n}
K_h(X_i-X_j)Y_i
}{
\sum_{i=1}^{n}
K_h(X_i-X_j)
}
$$
其中
$$
K_h(u)
=
\frac1hK\!\left(\frac{u}{h}\right)
$$
为核函数

然后构造插补Score ψ̄₁。对于未标记样本，构造
$$
\bar\psi_j(\delta)
=
(X_j-\delta)\mathbf1(X_j\ge\delta)
(A_j-\hat\pi_j)
(\hat Y_j-\hat h_j).
$$

半监督Score定义为两者的加权平均：
$$
\Psi_{SS}(\delta)
=
\lambda
\frac1n
\sum_{i=1}^{n}
\psi_i(\delta)
+
(1-\lambda)
\frac1N
\sum_{j=1}^{N}
\bar\psi_j(\delta)
$$

无标签数据通过协变量分布信息贡献于方差缩减。

## 检验统计量

我们采用Sup-type检验统计量，在候选区间内搜索所有可能的拐点位置，对于单个delta，标准化score统计量是：
$$
T(\delta)
=
\frac{\sqrt{n}\,\Psi_{SS}(\delta)}{\hat{\sigma}_{SS}(\delta)}
$$
因此sup-type检验统计量为：
$$
T_{n,SS}
=
\sup_{\delta \in D}
\left[
\frac{\sqrt{n}\,\Psi_{SS}(\delta)}{\hat{\sigma}_{SS}(\delta)}
\right]^2
$$
通过平方去掉正负号。Sup-type 检验通过在候选拐点集合 D 上逐点计算标准化 score 统计量，并取其上确界，从而将未知拐点问题转化为“最有利于拒绝原假设的最坏情形检验”，避免了对拐点的先验估计，同时保证对结构突变位置的鲁棒识别能力

## 乘数自助法
统计量同时包含 Sup 操作、未知拐点 δ 以及半监督复杂结构，使得传统渐近理论难以直接适用。首先，Sup-type 统计量涉及在整个参数空间上取上确界，本质上对应一个随机过程的极值分布问题，而该类分布通常不具备解析形式；其次，拐点 δ 作为 nuisance parameter 未知且非规则参数，其估计误差会影响统计量的稳定性，使得标准 Wald 或 Delta 方法不再适用；最后，在半监督框架下，score 函数同时包含核插补、倾向得分和结果回归等多个估计步骤，导致影响函数结构高度复杂且存在非独立同分布的依赖关系。因此，该问题的核心困难在于：需要在未知结构参数与高维随机过程极值分布的共同作用下，构造一个既具有渐近有效性又计算可行的检验方法。

乘数自助法巧妙地解决了这个问题：不重抽样数据，而是给每个样本施加随机权重，通过权重的随机扰动来模拟统计量的分布。具体做法：
$$
\Psi_{SS}^{*}(\delta)
=
\frac{1}{n}
\sum_{i=1}^{n}
\xi_i \psi_i(\delta)
+
\frac{1}{N}
\sum_{j=1}^{N}
\xi_j \bar{\psi}_j(\delta)
$$
$$
\xi_i \sim \text{i.i.d.}, \quad E(\xi_i)=0, \quad \mathrm{Var}(\xi_i)=1
$$

这样每次自助仅需O(n)计算量，完全避免了大规模无标签数据带来的计算负担，同时数学上可以证明其与真实分布渐近等价。在搜索空间D上取上确界：
$$
T_{n,SS}^{*}
=
\sup_{\delta \in D}
\left[
\frac{\sqrt{n}\,\Psi_{SS}^{*}(\delta)}
{\hat{\sigma}_{SS}(\delta)}
\right]^2
$$

最后我们构建临界值以及检验决策规则
$$
c_{1-\alpha}
=
\inf \left\{
c:
P^{*}(T_{n,SS}^{*} \leq c) \geq 1-\alpha
\right\}
$$
$$
\text{Reject } H_0
\quad \text{if} \quad
T_{n,SS} > c_{1-\alpha}
$$
---
### 三、渐近性质与效率增益（2分钟）

接下来介绍方法的理论性质，这是本研究的核心贡献。

**首先是正则条件**。

为确保渐近理论的严格性，我们引入七个正则条件：

- (C1)-(C2)：估计方程存在唯一解，参数估计具有渐近线性性
- (C3)-(C4)：条件方差有界，拐点搜索区间内有足够样本
- (C5)：样本量比例 $n/N \to \rho$，兼容无标签数据主导的情形
- (C6)：带宽条件 $h_n \to 0$, $nh_n \to \infty$, $nh_n^4 \to 0$，保证核估计无偏
- (C7)：影响函数类为P-Donsker类，保证经验过程的收敛性

这些条件是对标准半参数理论和经验过程条件的自然推广。

---

**定理1（极限分布）**：

在 $H_0: \tau = 0$ 下，假设倾向分数模型或结果模型至少一个正确设定，则

$$
T_{n,SS} \xrightarrow{d} \sup_{\delta \in \mathcal{D}} G_{SS}^2(\delta)
$$

其中 $\{G_{SS}(\delta) : \delta \in \mathcal{D}\}$ 是均值为零的高斯过程。

**定理1的意义**：
1. 确认了检验的双重稳健性——任一模型正确即可保证Type I Error控制
2. 极限分布是非标准的（高斯过程的上确界），需要通过自助法获得临界值

---

**定理2（效率增益）**：

这是本研究的核心理论贡献。设 $\Sigma_1(\delta)$ 为仅使用标记数据的Score方差，$\Sigma_{SS}(\delta)$ 为半监督Score方差，则

$$
\Sigma_{SS}(\delta) = \Sigma_1(\delta) - \Delta(\delta)
$$

其中 $\Delta(\delta) \geq 0$，且当 $\text{Var}(E[\psi_1 | h(X,W)]) > 0$ 时严格大于零。

**直觉理解**：
- 无标签数据提供了协变量分布 $P(X,W)$ 的精确信息
- 通过核估计插补 $E[Y|h(X,W)]$，利用了条件期望的结构
- 方差缩减量 $\Delta(\delta)$ 正是协变量分布信息对Score方差的"贡献"

打个比方：传统方法只看到"局部"，半监督方法看到了"全局结构"，因此估计更精确。

---

**定理3（功效提升）**：

在局部替代假设 $H_{1n}: \tau = n^{-1/2}c_0$ 下，检验统计量收敛于

$$
T_{n,SS} \xrightarrow{d} \sup_{\delta \in \mathcal{D}} [G_{SS}(\delta) + \mu_{SS}(\delta)]^2
$$

其中非中心参数（漂移函数）为

$$
\mu_{SS}(\delta) = \frac{c_0 \cdot E[\pi(1-\pi)(X-\delta)(X-\delta_0)\mathbf{1}(X \geq \max\{\delta, \delta_0\})]}{\sigma_{SS}(\delta)}
$$

**关键发现**：
- 分子（信号强度）与传统方法完全相同
- 分母 $\sigma_{SS}(\delta) < \sigma_1(\delta)$（由定理2保证）
- 因此 $\mu_{SS}(\delta) > \mu_1(\delta)$，**非中心参数被放大**

这意味着：在相同的真实效应下，半监督方法有更大的概率拒绝原假设，即**检验功效更高**。

---

**理论贡献总结**：

| 定理 | 结论 | 意义 |
|------|------|------|
| 定理1 | 极限分布为高斯过程上确界 | Type I Error可控，双重稳健性成立 |
| 定理2 | $\Sigma_{SS}(\delta) < \Sigma_1(\delta)$ | 融入无标签数据必然降低方差 |
| 定理3 | $\mu_{SS}(\delta) > \mu_1(\delta)$ | 方差缩减转化为功效提升 |

这三个定理构成了半监督双重稳健检验的完整理论框架，填补了现有文献的空白。


### 三、研究创新（1.5分钟）

本文有两个主要创新点：

第一个创新：构建了面向 kink 结构的半监督双重稳健检验统一理论框架

不同于传统仅考虑参数或半参数模型的检验方法，本文首次在 kink 异质性结构下，将双重稳健性、半监督学习与 Sup-type 非光滑检验统一纳入同一理论框架中，形成一个完整的可检验体系。

具体而言，我们系统刻画了三类核心性质：

1.在非规则参数（未知拐点 δ）存在下，建立了检验统计量的非标准极限分布理论；

2.在模型可能错设的情况下，通过 score 构造保证了双重稳健的渐近正态性结构；

3.在 Sup-type 非光滑泛函下，证明了统计量仍具有可用于推断的极限过程表示。

这一部分的核心贡献在于：将 kink 异质性检验从“单一参数推断问题”推广为“非光滑函数空间上的稳健推断问题”。

**第二个创新：严格证明了半监督效率增益的理论结果。**

我们不仅证明了方差缩减的存在性，还给出了Δ(δ)的具体表达式，揭示了效率增益来源于无标签数据中协变量分布的额外信息。这在现有文献中填补了半监督检验理论的空白。

---

### 四、模拟实验结果（2分钟）

接下来展示模拟实验的结果。

**模拟设计方面**，我们考虑了多种数据生成过程：

- 运行变量X分别从Uniform(-1,1)和N(0,1)分布生成
- 协变量W从二元正态分布生成
- 结果模型设置不同噪声水平σ = 0.5和σ = 1.0
- 样本量设置n = 200和n = 400，无标签与有标签样本比例为N/n = 5

我们对比了三种方法：
1. **Labeled-only**：仅使用标记数据
2. **Semi-supervised**：使用标记和未标记数据
3. **Oracle full-data**：假设所有Y都已知（作为理论上界）

**Type I Error控制方面**：

从表格可以看出，三种方法在原假设τ = 0下的拒绝率均围绕显著性水平α = 0.05波动，说明检验水平控制良好。无论是均匀分布还是正态分布的运行变量，结果都稳健。半监督方法的Type I Error与仅标记数据方法相近，验证了双重稳健性质。

**检验功效方面**：

随着处理效应τ从0.3增加到0.8，三种方法的拒绝率均显著上升，验证了检验的一致性。样本量从n = 200增加到n = 400后，功效整体提高，符合渐近理论预期。

Oracle full-data方法具有最高功效，因为它使用了全部响应变量信息，代表了检验功效的上界。Semi-supervised方法相对于Labeled-only方法在多数设置下保持接近或略有改进，验证了融入无标签数据的有效性。

**主要结论**：
1. 原假设下检验水平总体可控
2. 备择假设下拒绝率随kink强度和样本量增加而上升
3. 无标签样本中的协变量信息能够在一定程度上改善检验表现

---

### 五、实证应用与后续计划（1.5分钟）

最后介绍实证应用计划和后续研究安排。

**实证应用方面**，计划在两个经典数据集上验证方法：

1. **ACTG175数据集**：来自HIV治疗临床试验，包含2139名患者，四种抗逆转录病毒治疗方案。运行变量为基线CD4细胞计数，将检验治疗效应是否存在拐点型结构性变化。

2. **PBC数据集**：原发性胆汁性胆管炎研究，包含424名患者，D-青霉胺治疗。运行变量为血清胆红素，将检验治疗对生存的影响是否存在拐点。

**时间安排方面**：

- 2026年6-7月：文献深度研读与定理证明推导
- 2026年8-9月：完善模拟实验、扩展数据生成过程设置
- 2026年10-11月：完成ACTG175和PBC数据集的实证应用，撰写论文初稿
- 2026年12月至2027年5月：修改完善论文，准备答辩

---

**以上是我的开题报告，请各位老师批评指正，谢谢！**

---

## 全流程
Kink模型
↓

检验 H0:τ=0
↓

τ和δ联合估计困难
↓

改用Score检验
↓

原假设下构造矩条件
↓

得到基础Score
↓

引入倾向分数
↓

得到双重稳健Score
↓

利用无标签数据插补
↓

得到半监督Score
↓

δ未知
↓

Sup统计量
↓

Multiplier Bootstrap
↓

得到临界值
↓

完成检验

## 问题理解
首先要拟合y函数，然后根据拟合的y函数去检验$\tau=0$是否成立，但是因为拟合y的时候，delta未知，所以对拟合困难很大，因此构建score函数来拟合y，不估计tau，通过score可以知道固定的delta下，y的值会是多少，之后再去估计拐点